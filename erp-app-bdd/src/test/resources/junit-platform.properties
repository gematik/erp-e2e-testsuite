cucumber.execution.parallel.enabled=false
cucumber.execution.parallel.config.strategy=dynamic
cucumber.plugin=io.cucumber.core.plugin.SerenityReporterParallel,pretty
# Note: cucumber.glue is needed here as well so the maven-failsafe-plugin discovers only the app glue code
cucumber.glue=de.gematik.test.erezept.lei.steps,de.gematik.test.erezept.app.steps